<launch>
    <arg name="configuration" default="airgait" description="Tuning configuration to use. Must be a .yaml in the config/(arg robot)/ directory"/>
    <arg name="linear" default="true" description="Whether to linearize the change in PID values"/>
    <arg name="slope" default="100.0" description="The slope of the linear change in PID values. Only used when 'linear' is true"/>
    <arg name="robot" default="march6" description="Robot to use."/>

    <node name="march_gain_scheduling_node" pkg="march_gain_scheduling" exec="march_gain_scheduling" output="screen">
        <param from="/home/george/march/ros2/src/control/march_gain_scheduling/config/march6/airgait.yaml"/>
        <param name="~linearize_gain_scheduling" type="bool" value="true"/>
        <param name="~linear_slope" type="float" value="100"/>
        <param name="~configuration" type="str" value="march6"/>
    </node>
</launch>
